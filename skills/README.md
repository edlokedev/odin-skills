# Skills

Canonical Odin skills live here.

Run `.odin/scripts/sync-skills.sh` to project these skills into `.agents/skills` and `.claude/skills`.

Fallback when bash is unavailable:

```bash
bun run .odin/scripts/sync-skills.js
```
